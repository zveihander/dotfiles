# -*- mode: snippet -*-
# name: +page.server.ts load
# key: sload
# --
import type { PageServerLoad } from './$types';

export const load: PageServerLoad = async ({ ${1:params, locals} }) => {
	$0
	return {
		${2:data}
	};
};